#include <bits/stdc++.h>
using namespace std;
using lint = long long int;

int main()
{   
    cout.sync_with_stdio(false);
    cin.tie(nullptr);

    lint n, l, r, ai;
    cin >> n >> l >> r;
    for (int i = 0; i < n; i++)
    {
        cin >> ai;
        for (lint y = l; y <= r; y++)
        {

        }
    }
}